{
  "name": "util-monorepo",
  "version": "0.0.1",
  "description": "```shell\r npm install pnpm -g\r # 然后创造一个 新文件夹，在这个新文件夹中\r pnpm init\r ```",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "tsc   --project ./ --watch",
    "build":"tsc --outDir ./dist  --project ./ --watch"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.6.0"
  },
  "devDependencies": {
    "typescript": "^5.2.2"
  }
}
